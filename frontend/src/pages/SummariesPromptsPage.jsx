import PromptListPage from './PromptListPage';

export default function SummariesPromptsPage() {
  return (
    <PromptListPage
      kind="summaries"
      title="Summaries Prompts"
      subtitle="Global prompt library shared across every summaries bot. Pick one per schedule."
    />
  );
}
