import PromptListPage from './PromptListPage';

export default function YoutubePromptsPage() {
  return (
    <PromptListPage
      kind="youtube"
      title="YouTube Prompts"
      subtitle="Global prompt library for YouTube channels and keyword trackers. The first prompt is the default."
    />
  );
}
